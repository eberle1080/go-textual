# dom

Package `dom`. DOM tree and CSS selector matching. `DOMNode` tracks parent-child widget relationships and CSS class/ID attributes, enabling CSS-style queries (`query.go`) and selector matching against the live widget tree.
