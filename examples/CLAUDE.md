# examples

Not a Go package — this directory only contains subdirectories. Each subdirectory is a standalone `package main` example application demonstrating go-textual features. Run any example with `go run ./examples/<name>`. Current examples: `hello`, `counter`, `calculator`, `stopwatch`, `todo`, `dashboard`, `file_browser`.
