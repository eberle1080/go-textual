# validation

Package `validation`. Input validation rules and result types used by form widgets like `Input`. Validators are functions that accept a string value and return a `ValidationResult` indicating success or a failure message.
