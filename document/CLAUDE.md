# document

Package `document`. Text document model backing the `TextArea` widget. Provides a mutable text buffer (`document.go`), edit operations (`edit.go`), undo/redo history (`history.go`), cursor navigation (`navigator.go`), word-wrapped views (`wrapped.go`), and optional syntax highlighting (`syntax.go`).
