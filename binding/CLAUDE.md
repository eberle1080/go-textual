# binding

Package `binding`. Key binding management. Defines keyboard shortcuts, maps key names to actions, and tracks which bindings are currently active. Used by widgets and the app to handle user input via named actions rather than raw key codes.
