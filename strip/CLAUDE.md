# strip

Package `strip`. Immutable styled text line primitives. A `Strip` represents one horizontal row of terminal output as a sequence of Rich-styled text segments. It is the unit returned by `Render()` — one `Strip` per screen row within a widget's region.
