# keys

Package `keys`. Key name constants and key type definitions. Provides the canonical string names for keys (e.g., `"ctrl+c"`, `"enter"`, `"f1"`) that are matched against key bindings and used in `KeyMsg`.
