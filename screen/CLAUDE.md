# screen

Package `screen`. Defines the `Screen` interface — the root widget of every go-textual application. Implement `OnMount() Cmd` to initialize the app when it first starts. A `Screen` is composed and rendered like any other widget but serves as the top of the widget tree.
