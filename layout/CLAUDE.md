# layout

Package `layout`. Layout algorithms that partition screen regions among child widgets. Implements `vertical`, `horizontal`, `grid`, and `stream` strategies. `arrange.go` is the entry point; `factory.go` selects the algorithm from CSS properties; `placement.go` describes the output positions assigned to each child.
