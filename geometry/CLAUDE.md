# geometry

Package `geometry`. Geometric primitives used throughout the framework. Provides `Size` (width/height), `Region` (x/y/width/height rectangle), and `Offset` (x/y point) types — the fundamental units for layout and rendering calculations.
