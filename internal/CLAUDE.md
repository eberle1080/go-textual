# internal

Not a Go package — this directory only contains subdirectories. Houses internal implementation packages that are not exported to external consumers of go-textual. Sub-packages: `ansi`, `cache`, `layoutresolve`, `partition`, `resolve`, `spatial`, `xterm`.
