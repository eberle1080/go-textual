# internal/layoutresolve

Package `layoutresolve`. Resolves CSS-style dimension values (percentages, `fr` fractional units, fixed cells) into concrete integer sizes given a container's available space. Used by the layout algorithms before partitioning screen regions.
