# internal/resolve

Package `resolve`. Resolves inherited and computed style property values by walking up the DOM tree. Bridges the CSS and DOM packages so that widgets receive their final effective styles before rendering.
