# internal/partition

Package `partition`. Algorithms for partitioning a linear space (rows or columns) among a set of weighted segments. Core primitive used by the layout system to distribute available screen space among child widgets.
