# internal/ansi

Package `ansi`. ANSI escape sequence constants and helper functions. Provides the raw escape strings for cursor movement, color output, screen clearing, and other terminal control codes used by the rendering and driver layers.
