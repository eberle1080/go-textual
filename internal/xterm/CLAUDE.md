# internal/xterm

Package `xterm`. XTerm/VT100 input parser. Decodes raw terminal byte streams — including ANSI escape sequences, Kitty keyboard protocol, and mouse reports — into structured `KeyMsg` and `MouseMsg` events consumed by the event loop.
