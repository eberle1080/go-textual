# color

Package `color`. Color model, named color constants, and LAB color space conversions. Provides the `Color` type used throughout the CSS and rendering systems for specifying foreground and background colors.
